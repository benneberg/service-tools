# Either set X-Auth (preferred) OR a Bearer token
# Format for X-Auth: tokenId:tokenSecret
SIGNAGEOS_X_AUTH=your_token_id:your_token_secret
# Or
SIGNAGEOS_API_KEY=your_bearer_token

# Optional override of API base (rare)
# SIGNAGEOS_API_BASE=https://api.signageos.io/v2
